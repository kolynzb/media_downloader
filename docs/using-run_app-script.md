# Running Application with Bash Script (Option 1)

- You can start the application using the `run_app.sh` bash script.
- Make Bash Script executable.
  `chmod +x run_app.sh `
- To run executable.
  `./run_app.sh `
